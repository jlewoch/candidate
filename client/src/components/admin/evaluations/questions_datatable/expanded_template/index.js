import QuestionsExpandedTemplateDisplay from './QuestionsExpandedTemplateDisplay'
import { connect } from 'react-redux'

const mapStateToProps = state => ({})
const mapDispatchToProps = dispatch => ({})

export const QuestionsExpandedTemplate = connect(
  mapStateToProps,
  mapDispatchToProps
)(QuestionsExpandedTemplateDisplay)
