import QuestionsDatatableDisplay from './QuestionsDatatableDisplay'
import { connect } from 'react-redux'

const mapStateToProps = state => ({})
const mapDispatchToProps = dispatch => ({})

export const QuestionsDatatable = connect(
  mapStateToProps,
  mapDispatchToProps
)(QuestionsDatatableDisplay)
